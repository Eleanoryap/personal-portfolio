/** Decorative section divider in the terminal-readout idiom. */
export function TerminalRule() {
  return (
    <div className="rule" aria-hidden="true">
      - - - - - - - - - - ===========++
    </div>
  );
}
